# 全庫內容審核報告（2026-09-03）

六批 subagent 逐篇審核 310 篇的原始報告彙整。「疑似問題但未改」段落是需要人工決定的項目。

# architecture／database 逐篇審核報告

審核範圍：`architecture/`、`database/` 兩目錄（含子目錄）所有 .md，略過 README.md，共 49 篇。

## 已修改

### database（本人親審）

- database/sql-server/SQL LIKE.md：錯字「查詢需球」→「查詢需求」。
- database/sql-server/SQL Server 刪除資料庫中所有資料表.md：cursor 的來源 `INFORMATION_SCHEMA.TABLES` 未過濾檢視表，`DROP TABLE` 遇到 view 會失敗，補上 `TABLE_TYPE = 'BASE TABLE'` 條件。
- database/sql-server/SQL Server 刪除資料庫中所有資料表.md：破壞性語法缺警語，於標題下補一行 ⚠️ 警示（先確認資料庫、需有可還原備份）。
- database/sql-server/SQL Server 版本檢查與 Compatibility Level 設定指南.md：EF Core 對照表「EF Core 3.x 需使用 System.Data.SqlClient」→「Microsoft.Data.SqlClient」（EF Core 3.0 起已改用 Microsoft.Data.SqlClient）。
- database/sql-server/從 SQL Server 2008 升級至 Azure SQL 的完整指南.md：Q2 跨資料庫查詢的建議寫法錯誤——Azure SQL Database 不支援三段式 `[db].[schema].[table]`，改為說明需用 Elastic Query（External Data Source／External Table），並補註 Managed Instance 原生支援。
- database/sql-server/使用 Data Migration Assistant 移轉 SQL Server 資料庫與帳戶.md：繁中文件內殘留「數據」→「資料」（2 處）。
- database/sql-server/SQL Server 撰寫Stored Procedure小細節.md（reprint）：錯字「讓費時間」→「浪費時間」、「stroed procedure」→「stored procedure」。
- database/sql-server/SQL Server 撰寫Stored Procedure小細節.md（reprint）：3 個程式碼區塊在轉貼時被壓成單行導致 SQL 語法壞掉（`--` 註解吃掉後續語句、`st.textFROM`、`cached_plans capCROSS APPLY`），還原換行。
- database/sql-server/使用SSIS創建同步資料庫數據任務.md（reprint）：「Intergration Services Project」→「Integration」、「OLE DB Destionation」→「Destination」、「將 Merge Sort 中的輸出」→「Merge Join」（前文一路都是 Merge Join）、參數佔位符全形「？號」→半形「?號」。
- database/dbeaver/DBeaver 在 Mac 上使用英文介面.md（reprint）：shell script 區塊 `#!/usr/bin/env bash"$(dirname "$0")/dbeaver" -nl en` 缺換行導致腳本壞掉，還原為兩行；路徑「`/Application`」→「`/Applications`」。
- database/sql-server/SSMS v18 清除登入畫面中，下拉選單歷史紀錄.md（reprint）：路徑 `C:\Users[使用者名稱]\` 漏一個反斜線。
- database/sap/ABAP開發環境和總體介紹.md：標題殘留 markdown 記號「## 2. ABAP\*\*開發總體介紹」→ 移除 `**`；TCODE 表「SE37 函數據模塊」→「函數模塊」（簡繁轉換造成的字串汙染）。
- database/sap/SAP MM模塊常用表總結.md：表名／錯字訂正——「KNVV 銷售試圖」→「銷售視圖」、「MVKB 銷售視圖」→「MVKE」（MVKB 不存在，同文他處作 MVKE）、「銷售訂單 VBKA 擡頭」→「VBAK」（VBAP 為其行項，表頭應為 VBAK）。
- database/sap/SAP欄位與表的對應關係.md：「LBC1 Vendor Master – Transaction Figures」→「LFC1」（LBC1 不存在，同 repo 另一篇作 LFC1）。

### architecture（本人親審的 original）

- architecture/ddd-cqrs/MediatR Notification (Publish) 與 Behaviors 範例.md：`cfg.AddBehavior(typeof(IPipelineBehavior<,>), typeof(XxxBehavior<,>))` 對開放泛型 behavior 是錯誤 API，改為 MediatR 12 正確的 `cfg.AddOpenBehavior(typeof(XxxBehavior<,>))`（3 行）。
- 同上：註解「發布事件…（非同步並行執行）」與文末重點整理「Publish 順序：預設循序執行」自相矛盾，改為「（預設循序執行）」。
- 同上：TransactionBehavior 區塊以 ` ```csharp ` 開頭卻用 `~~~csharp` 開＋` ``` ` 收，造成後續「## 3. 完整 Pipeline 執行流程圖」整段被吞進程式碼區塊，已修正圍籬配對（全庫 fence 平衡已驗證）。
- architecture/microservices/淺談微服務與網站架構的發展史.md：錯字 7 處——「稍後在敘述」→「再敘述」、「具備可以性」→「可用性」、「例外一個節點」→「另外一個節點」、「在散布式架構中」→「分散式」、「由於該將架構缺乏統一管理」→「由於該架構」、「SOA 可能由涉及」→「可能會涉及」、「那麼既使我們」→「即使」。

### architecture（reprint，委派子代理審核）

以下為三批子代理逐篇審核後所修，均限於客觀錯誤（錯字、壞掉的程式碼／連結、錯誤 API 與產品名），未動作者用語、觀點與結構。

**批次 A（design-patterns 4 篇、auth CAS、微服務架構 #2、從狀態圖驅動 API、架構師觀點 Part #1／#2，共 49 處）**

- 單例模式 (Singleton Pattern).md：`statc`→`static`；「簡單執行緒安全 Singleton」區塊掉了類別宣告行（欄位裸露、大括號不成對）已補；「不使用 Lock」區塊多一個 `}` 已移除。
- 軟體分層設計模式.md：`Repoitory`→`Repository`、`Parmeter`→`Parameter`、`Coontroller`→`Controller`。
- 雙重檢查鎖定模式.md：`Contorller`→`Controller`；第一段 Controller 範例掉了 method 宣告（建構式後直接接程式碼、大括號不成對），依下一段同型範例補回 `[HttpGet] public ActionResult<string> Get()`。
- Repository 模式.md：`Repoistory`／`Repoitory`→`Repository`；`public class GenericRepository : GenericRepository<TEntity>`（類別繼承自己且缺型別參數）→ `GenericRepository<TEntity> : IGenericRepository<TEntity>`；UnitOfWork 區塊多餘 `}` 移除。
- Single Sign On (CAS).md：`managr`→`manager`；「這裡在快速把流程帶過」→「再快速」。
- 微服務架構 #2.md：「切割為涵式庫」→「函式庫」。
- 從狀態圖來驅動 API 的設計.md：錯字 18 處（間皆→間接、以啟用→已啟用、在回報→再回報、抽象畫→抽象化、subscribtion→subscription、綁訂→綁定、定用帳戶→訂用帳戶…等）。
- Part #1 改變架構的動機.md：`monolitch`→`monolithic`、洽當→恰當（2）、一項都是→一向都是、相依姓→相依性、`componet`→`component`、重構測略→重構策略（3 個小節標題）、隔閤→隔閡等。
- Part #2 實際改變的架構案例.md：`pooling`→`polling`（2 處，指輪詢）、`Maintainess Guide`→`Maintenance Guide`。

**批次 B（基礎建設 Part #1–#5、容器化 #1／#2、API First #1／#2，共 91 處）**

- Part #1 Service Discovery.md：`end ponts`→`end points`；「開發集除錯」→「開發及除錯」；**AWS ELB 與 Azure Load Balancer 兩條外部連結左右對調**（AWS 指向 docs.microsoft.com、Azure 指向 aws.amazon.com），已互換回正確網址。
- Part #2 服務負載的控制.md：這方是→這方式、分散是版本→分散式、為運→維運、條配→調配、`servics`→`services`。
- Part #3 排隊機制設計.md：`IPOS`→`IOPS`（2 處）；值到→直到（2）、維持再→維持在、一值→一直、不得以→不得已等。
- Part #4 Message Queue Based RPC.md：`stroage`／`subscribtion`／`sequency diagram`／`Operaion`／`managment`／`producere`／`Signletion` 等英文拼字；多處中文錯字；3 處反引號錯位導致 markdown 壞掉；**docker-compose 指令複製貼上錯誤**——內文說「2→1」「1→0」但兩段都寫 `--scale consumer=2`，已改為 `consumer=1`／`consumer=0`。
- Part #5 Process Pool.md：`frameowkr`→`framework`、「.NET code」→「.NET core」、`WorkerMan()`→`WorkerMain()`、表格標頭 `Process .NET Core)` 補左括號（2 處）、測式／測是→測試（8 處）等。
- 容器化 #1.md：「含式庫」→「函式庫」、「就可 已執行」→「就可 以執行」。
- 容器化 #2.md：正卻→正確、公具→工具、竹條→逐條、正長→正常；`docker run -d –restart always` 全形連字號 → `--restart always`；**兩處「Tips: 處理 Shutdown 事件」連結指向本文自身**，改為同文第三處出現的正確網址。
- API First #1 觀念篇.md：職播→直播、正的→真的、在來→再來、洽當→恰當、M 行→M 型、像前相容→向前相容、正式→正視等。
- API First #2 設計實做篇.md：「PI First」→「API First」、`Component O bject Model`→`Component Object Model`、`paramenters`／`metthod`／`objserver pattern` 拼字；**程式碼區塊語言標記 ` ```meriaid ` → ` ```mermaid `**（否則圖不會渲染）；處裡→處理（5 處）等。

**批次 C（架構面試題 #1–#4、API & SDK Design #1–#5，共 82 處）**

- API & SDK Design #3.md：**貼上損壞**——`~~~InvalidOperationException```:` 把行文與 csharp 區塊壓在一起、且以 `~~~` 收尾，已還原；另修 3 處錯字。
- API & SDK Design #4.md：**貼上損壞**——`~~~http://localhost:56648/swagger/docs/v1````` 把網址與 json 區塊黏成一團，已還原；「事倍功半」→「事半功倍」（語意相反）；上線→上限、規再一起→歸在一起等。
- API & SDK Design #5.md：標題 `# API & SDK Design# 5` → `# API & SDK Design #5`；道別→到別、驗證玩→驗證完、偷倒→偷到、一但→一旦、`publc key`→`public key`。
- API & SDK Design #1／#2.md：多於→多餘；耀這要做→要這樣做；都已→都以。
- 架構面試題 #1.md：納為了→那為了；`microsoervice`→`microservice`。
- 架構面試題 #2.md：語序損壞「降低運重複的算量」→「降低重複的運算量」（3 處）；**`Azure Stream Analystic(s)`→`Azure Stream Analytics`（2 處）**；**`ThumblingWindow`→`TumblingWindow`**（與其上方 SQL 範例一致）；免強→勉強、不得以→不得已、得不嘗失→得不償失。
- 架構面試題 #3.md：**`Oracle (CONNECT TO)`→`Oracle (CONNECT BY)`**；`Postgras`→`Postgres`；各數→個數等 5 處錯字；**文末參考資料一條連結被截斷、另有一行殘缺網址片段**，已合併還原為完整網址並移除殘行。
- 架構面試題 #4.md：中文錯字 28 處、英文／識別字拼字 6 處（`Inheritence`→`Inheritance`、`workround`→`workaround`、`Tuble`→`Tuple`、`Catagory`→`Category`、`SpeciaOffer`→`SpecialOffer`）；商品表格價格欄 `#139`→`$139`（2 處）。

## 疑似問題但未改

### database

- database/sql-server/SQL Server 移除資料庫中所有的關聯限制.md：**標題與內容不符**。標題與另一篇「刪除所有資料表」的交互引用都說要「移除所有關聯限制（FOREIGN KEY）」，但腳本實際只針對複寫欄位 `msrepl_tran_version` 產生 DROP DEFAULT CONSTRAINT／DROP COLUMN 的語句，完全沒碰 FK。要修等於重寫整段腳本，超出外科手術範圍，故只回報。
- database/sql-server/使用 Data Migration Assistant 移轉 SQL Server 資料庫與帳戶.md：Step 3 的建帳號腳本用 `Use [master]; … Use [databasename];`，但 Azure SQL Database 不支援在同一連線內 `USE` 切換資料庫，需分別連 master 與目標 DB 執行。作者可能是以「兩段分別執行」為前提，語意曖昧，未改。
- database/sql-server/從 SQL Server 2008 升級至 Azure SQL 的完整指南.md：`EXEC sp_updatestats;` 於 Azure SQL Database 的支援狀態我無法確認，未動。
- database/sql-server/SQL Server 版本檢查與 Compatibility Level 設定指南.md：Q2 稱「LINQ group by、cross apply 在相容性層級 100 上會失敗」——CROSS APPLY 自 SQL 2005 起即支援，此說法可疑，但屬作者經驗敘述，未改。
- database/sql-server/SQL Server 版本檢查與 Compatibility Level 設定指南.md：整份文件被包在一個 `- ` 清單項目底下縮排，屬格式風格，未動。
- database/sap/*.md（kind: unknown，第三方簡→繁轉換稿）：全篇存在轉換殘留用語（型別=类型、檢視=视图、屏幕、模塊、數據庫…）與少量條目重複（TQ32C、QPRVK、QPRVP 各重複一行）；SM35／SM50 的描述疑似對調（SM35 應為批次輸入監控、SM50 為進程監控）、CMOD/SMOD「系統增加」疑為「系統增強」、MSDE 展開應為 SQL Server Desktop Engine 而非 Microsoft Data Engine。因非本人原創且屬全文用語問題，僅回報不改。

### architecture

- architecture/ddd-cqrs/DDD + CQRS + MediatR 專案架構.md：**全檔沒有任何程式碼圍籬（0 個 ```），也沒有 H1 標題**，整份 C# 會被當作內文段落渲染。要修得在頭尾加 fence，屬格式變更，先回報。
- architecture/ddd-cqrs/MediatR ….md：文末提到的 `ParallelNoWaitPublishStrategy` 並非 MediatR 內建型別（MediatR 12 是設定 `cfg.NotificationPublisher = new TaskWhenAllPublisher()`），但可能來自社群範例，未改。
- architecture/auth/OAuth 與 SSO.md：檔名含 SSO 但內文 H1 只有「# OAuth」、無 SSO 段落；且「## 目的 - 身份驗證」在概念上有誤（OAuth 2.0 是授權 Authorization，身分驗證是 OIDC）。此檔是使用者的大綱草稿，改動等於改寫觀點，未動。
- architecture/microservices/淺談微服務與網站架構的發展史.md：「業務項目就如上述例子中的『商品服務』、『訂單服務』以及『商品服務』」——「商品服務」重複，正確項目無法推定。
- 同上：「每一節點…意即它們都嘗試一個整體的服務」，「嘗試」明顯是別字（疑為「構成」），替代字不唯一，未改。
- 同上：全文散見簡體來源用語（數據／信息／接口／緩存／程序），但作者用得一致（如「遠端程序調用」為專有名詞），全面轉換會變成重寫，未動。
- architecture/microservices/微服務架構 - 從狀態圖來驅動 API 的設計.md：**H1 標題結尾多了「 OK」**（`# … API 的設計 OK`），疑似整理時留下的審閱標記；不確定是否刻意保留，未動。
- 同上：全文約 7 處把 `FSM` 寫成 `FSA`；狀態清單／enum／轉移表三處名稱互不一致（REGISTERED/VERIFIED/LOCKING vs CREATED/ACTIVATED/DEACTIVATED，`VerifyEmail()` vs `ValidateEmail()` vs `"EmailValidate"`）；`DDD (Domain Driven Development)` 應為 Design。皆屬原作內部矛盾，未改。
- architecture/design-patterns/雙重檢查鎖定模式.md：`this._cache.Get<DateTime>("key") == null` 中 `DateTime` 是實值型別，與 null 比較無法編譯／恆為 false，應用 `TryGetValue` 或 `Get<DateTime?>`。屬原作邏輯錯誤，非貼上損壞。
- architecture/design-patterns/單例模式.md：`DoubleCheckedLockingSingleton` 缺 private 建構式，實際擋不住外部 new。
- architecture/design-patterns/Repository 模式.md：`IBlogRepository` 介面與實作的回傳型別／參數對不上（`Task<int>` vs `Task<bool>`、`RemoveAsync(Blog)` vs `RemoveAsync(int)`）；「Service 的應用」下的程式碼區塊是空的；另一處注入設定區塊被截斷缺結尾大括號。
- architecture/auth/Single Sign On (CAS).md：`if (username !== "123" && password !== "123") return res.redirect("/bad")` 的 `&&` 應為 `||`，否則只錯一項仍能登入；DEMO 區的 `<video>` 標籤沒有 `src`。屬原作 demo code 的 bug。
- architecture/microservices/基礎建設 Part #2.md：`StatisticEngineThrottle`（內文與 new 的地方）與 `StaticEngineThrottle`（類別定義）三處名稱不一致；全文以 `peek` 指尖峰流量（應為 `peak`）；`TokenBucketThrottle` 用 `>` 疑應為 `>=`。
- architecture/microservices/基礎建設 Part #3.md：顏色描述前後矛盾（前文紅=用餐區、藍=排隊區，後文說「還留在排隊區 (紅色)」）；全篇以 `pooling` 指 polling。
- architecture/microservices/基礎建設 Part #4.md：「RPC 透過網路，通常回應時間都遠低於正常的本地呼叫」語意上應為「遠高於」；程式碼參數 `TInputMessage intput` 疑為 input（多處一致）。
- architecture/microservices/基礎建設 Part #5.md：`if (args != null & args.Length > 0)` 用單一 `&`（非短路），`args` 為 null 時會 NRE；benchmark 輸出兩行同名 `SingleProcessWorker`；`Mode: BLOB/BASE64` 與內文「BASE (大量資料)」不一致。
- architecture/microservices/容器化 #1.md：`-d` 註為 `(–daemon)`，實際 docker 旗標為 `--detach`。
- architecture/microservices/建構微服務開發團隊/架構面試題 #2.md：示範 SQL 的 `between getdate() and dateadd(hour,-1,getdate())` 上下界寫反實際查不到資料（作者引為「面試者的答案」示意）；三段解法的 schema／C# 程式碼／執行 log 被塞進同一個 ``` 區塊；`TestConsole` 後方有一個完全空的程式碼區塊（內容遺失）。
- architecture/microservices/建構微服務開發團隊/架構面試題 #3.md：需求 4 的 T-SQL 用了未宣告的 `@root_node`（應為 `@dest_node`）；`*.dll` 範例的註解與 `F.EXT = '.ini'` 條件不一致；需求 5 的 SQL 區塊首行混入 shell 指令 `del /s c:\windows\backup`。
- architecture/microservices/建構微服務開發團隊/架構面試題 #4.md：`DiscountRule3/6` 的 `10 - percentOff / 10` 整數除法算出「9 折」與活動名「88 折」不符（但與原文 log 一致）；`[封裝](…Encapsulation_(computer_programming)))` 多一個右括號。
- architecture/microservices/建構微服務開發團隊/架構面試題 #1.md：無 lock 對照組的類別仍叫 `LockAccount`，行文與結果都稱 `WithoutLockAccount`；mongo 前／後兩張截圖用同一個圖片網址（依規定不動圖片連結）。
- architecture/microservices/實做基礎技術 API & SDK Design #2.md：行文說改成 `BirdNo`，程式碼寫 `BirdlNo`（多一個 l），無法判定哪邊為準。
- architecture/microservices/實做基礎技術 API & SDK Design #4.md：`diagnoistic`（應為 diagnostic）出現於自動產生的 JSON／註解中；`[http://{你的名字}.azurewebsites.net](http://xn--{}-1p3cj1pu0ku64b…)` 目標網址是轉檔時被 punycode 化的亂碼，無法推定正確網址。
- architecture/microservices/API First #1.md：稱貝佐斯為「AWS 的前 CEO」（實為 Amazon CEO）。
- 多篇（Part #4、從狀態圖驅動 API 等）：系列文連結網址內含空白與冒號（如 `/categories/#系列文章: Thread Pool 實作`），部分 markdown 解析器會斷掉，屬原文格式未動。

補充檢查結果：全庫未發現 `https:/` 這類缺斜線的網址；兩個相對圖片路徑（`architecture/design-patterns/images/SLA.png`、`RepositoryUnitOfWorkBetter.png`）檔案皆存在；修改後全部 49 篇的程式碼圍籬（``` 與 ~~~）配對已重新驗證平衡。

## 統計

審核 49 篇，修改 41 篇，共 249 處
# dotnet/aspnet-core 全目錄審核報告

審核範圍：`dotnet/aspnet-core/`（含 `net6-samples/`、`wdmis/`）全部 64 篇 .md（README.md 已略過）。

## 已修改

### 根目錄

- `ASP.NET 9 OpenAPI 介紹與教學.md`：修兩個壞掉的目錄錨點（`#整合-swaggerui`→`#整合-swagger-ui`、`#整合-scalarui`→`#整合-scalar-ui`）；`AddVersionedApiExplorer` 改為 Asp.Versioning 實際提供的 `AddApiExplorer`；ReDoc 的 `options.SpecUrl(...)` 改為屬性指派（`SpecUrl` 是 string 屬性，原寫法無法編譯）。
- `ASP.NET Core Authentication系列（一）…md`：`web.cofig` → `web.config`。
- `ASP.NET Core Authentication系列（二）…md`：`AuthorizeType="Basic"` → `AuthenticationType="Basic"`（ClaimsIdentity 建構子參數名）；「用戶註釋」→「用戶註銷」。
- `ASP.NET Core Authentication系列（四）…md`：程式碼註解「保存大文件夹」→「保存到文件夹」。
- `ASP.NET Core Consul 教學.md`：「Cunsul 架構圖」→「Consul 架構圖」（2 處）；第 8 節健康檢查 URL `localhost:5000` 改為與註冊時一致的 `localhost:7240`。
- `ASP.NET Core NLog-如何使用 NLog 將 log 寫到檔案.md`：「格視為」→「格式為」、`meassage`→`message`；參考資料最後一則壞掉的連結格式修好。
- `ASP.NET Core NLog-依照Environment使用Nlog.Config檔案.md`：`NLogBuilder.ConfigureLog` → `ConfigureNLog`。
- `ASP.NET Core Nlog-發送訊息到ElasticSearch.md`：「記得加上 roles」→「rules」；「只有3跟3可以正常運作」→「3跟4」（後文明說採用第 4 種）。
- `ASP.NET Core 網站生命週期.md`：`Boundle` → `Bundle`。
- `ASP.NET Core 中使用 HttpReports…md`：`MHttpReports.Dashboard` → `HttpReports.Dashboard`。
- `ASP.NET Core 呼叫 API 發生 CORS 錯誤.md`：「允取」→「允許」（2 處）、`CROS` → `CORS`。
- `ASP.NET RulesEngine 介紹.md`：規則 4 文字「大於300元」→「大於3000元」（JSON 為 `order.Amount > 3000`）；namespace `KH.Lab.RulesEnglineService` → `RulesEngineService`。
- `LinePay.md`：`options.payment = "capture=false"` 改為 LINE Pay v3 實際的巢狀物件 `payment = new { capture = false }`；`[HttpPost("confirm")]` → `[HttpGet("confirm")]`（前端 JS 與 LINE Pay 導回皆為 GET）。
- `ASP.NET Feature Management 使用說明與教學.md`：修兩處無法編譯的 `bool isAdmin = // ...` 賦值；內文與程式碼不一致的名稱統一為實際的 `IDisabledFeaturesHandler`／`HandleDisabledFeatures`／`FeatureNotEnabledHandler`；`AddFeatureFilter<PercentageFilter>()` 補完整命名空間（型別在 `Microsoft.FeatureManagement.FeatureFilters`）。
- `ASP.NET Core 分散式Session – 使用 Redis.md`：`$docker run –name` 全形連字號改為 `--name`；錯字 `Dockfile`→`Dockerfile`、`Ketral`→`Kestrel`、`Programs.cs`→`Program.cs`、`LoninController`→`LoginController`、`Reauest`→`Request`、`案裝`→`安裝`、`瀏灠器`→`瀏覽器`、`由其是`→`尤其是`、`洽當`→`恰當`、`將應.Net Core`→`將 .Net Core`。
- `ASP.NET Core Hangfire 排程管理.md`：修四處複製貼上殘字（`非同部步`、`Hangfire angfire`、`Console.Log 到onsole.Log 到`、`Hangfire Hangfire 介面`）；「執行過程當成被刪除」→「當中」；內文 `GetModuleType`→`GetModuleTypes`。
- `ASP.NET Core 使用 MassTransit 與 RabbitMQ…md`：`解偶`→`解耦`、`Kafaka`→`Kafka`、`Message Boker`→`Message Broker`（3 處）、`支多個`→`支援多個`、`合约`→`合約`、`quere`→`queue`；範例位置連結含空白會斷掉，改為 `Event%20Bus`。
- `ASP.NET Core Quartz.NET 管理介面.md`：`scpoe`→`scope`、`cornExpression`→`cronExpression`、`corn expression`→`cron expression`、`Sartup`→`Startup`、註解「註冊DB容器」→「註冊DI容器」、「布署在 IIS」→「部署在 IIS」、「黑暗執行續」→「黑暗執行緒」。
- `ASP.NET Core Quartz 介紹.md`：「它靈活而不復雜」→「不複雜」；程式碼字串 `"每3秒输出"`→`"每3秒輸出"`。
- `ASP.NET Core RDLC 報表設計.md`：`dotnet new web` → `dotnet new mvc`（原指令產生 minimal API，沒有 Controllers/Views）；`nameof(Employees)` → 字串 `"Employees"`（無法編譯）；檢視頁 `asp-controller="Home"` → `"Reports"`；`stream.GetBuffer()` → `stream.ToArray()`（GetBuffer 含尾端補零，圖片位元組會壞）；「應用程序」→「應用程式」。
- `ASP.NET Core KeyCloak 實作.md`：「WebAPI 項目」→「WebAPI 專案」。
- `ASP.NET Core 如何套網頁設計樣版.md`：程式碼區塊內 `\~/Scripts/...`、`\~/Content/...` 移除反斜線（圍籬內反斜線非跳脫，會原樣輸出成錯誤路徑），共 9 處。
- `ASP.NET Core SFTP 使用第三方套件 SSH.Net.md`：標題「第三方套建」→「第三方套件」；`class1`→`Class1`；`Upload` 的 XML 註解 `<param name="localPath">`→`localFile`（CS1572）；`sftpProt = "21"`→`sftpPort = "22"`（21 是 FTP 埠，SFTP 走 22，含呼叫處同步）；`sftpDownloadNewFileNme`→`sftpDownloadNewFileName`；「SshSftp 資料夾／勾起來」→ SshDotNet（專案名）；「剛剛製過來」→「剛剛複製過來」；註解「文件夾」→「資料夾」。
- `ASP.NET Core 客製化Model Validation 預設錯誤訊息.md`：ModelBindingMessage 表格三列（`MissingKeyOrValue`、`MissingRequestBodyRequiredValue`、`NonPropertyAttemptedValueIsInvalid`）的值整體位移一格已修正；重複出現的 `AttemptedValueIsInvalid` 改為缺漏的 `ValueMustBeANumber`；`SetValueMustBeANumberAccessor` 由 `NonPropertyValueMustBeANumber`（無 `{0}` 佔位符）改為 `ValueMustBeANumber`；多國語系範例補上缺漏分號；錯字「反映」→「向」、「必需」→「必須」、「正規表式」→「正規表示式」、「必須介在」→「必須介於」；移除檔尾空的 `######`。
- `ASP.NET Core 前台會員登入.md`：`JavsScript`→`JavaScript`；「呼叫 Member/DoLogin所以在」補回缺漏空格；目錄 1.1 行補回行尾兩空格（否則與下一行併段）。
- `ASP.NET Core 前台會員修改個人資料.md`：`JavsScript`→`JavaScript`。
- `ASP.NET Core 前台會員忘記密碼與重設密碼.md`：「程式碼會信件到會員信箱」補為「會寄信件」；「重新密碼範例」→「重設密碼範例」。

### net6-samples/

- `NET 6 Dapper範例.md`：`if (System.IO.File.Exists(...))` → `if (!System.IO.File.Exists(...))`（註解寫「找不到檔案時建表」但條件寫反，照原樣執行首次不建表、之後重複建表拋錯；同作者《Dapper語法說明》正是 `!Exists`）。
- `NET 6 Dapper語法說明.md`：「因為在在執行語法時」→「因為在執行語法時」。
- `NET 6 EFCore語法說明.md`：修壞掉的圖片 markdown（`![範例10-2](...png` 後斷行才接 `)`，圖片不渲染且文字黏行）。
- `NET 6 Hangfire範例.md`：套件名 `Quart.Net` → `Quartz.NET`。
- `NET 6 MailKit前置作業.md`：全形「ＭailKit」→「MailKit」。
- `NET 6 Moq範例.md`：「針對 xUnitExample.Tests 加入套件」→「MoqExample.Tests」（複製前篇殘留）；`Ilogger`→`ILogger`（2 處）。
- `NET 6 swagger授權.md`：JwtHelper 的 `建立JWT-Token` 與 Filter 的 `取得所有controller內的action` 兩行缺 `//`，直接貼上編譯失敗，已補；「使用有鎖頭的AP / I」接回 API；`強行別`→`強型別`。
- `NET 6 Serilog範例.md`：註解 `等極為warning` → `等級為warning`。
- `NET 6 取得appsettings檔案內容.md`：`強行別` → `強型別`。

### wdmis/

- `FeatureManagement.md`：內文說裝 `Microsoft.FeatureManagement.AspNetCore`，指令卻是 `Microsoft.FeatureManagement`，指令已對齊內文。
- `Serilog.md`：`Program.cs` 範例中 `ReadFrom.Configuration(configuration)` 的 `configuration` 從未宣告，補上 `ConfigurationBuilder` 建立與對應 using；`數據`→`資料`（2 處）、`調試`→`除錯`。
- `Spectre.Console.md`：`AnsiConsole.Render(table)` 自 0.42 起已淘汰，改為 `AnsiConsole.Write(table)`；`數據`→`資料`（3 處）、`用戶反饋`→`使用者回饋`、`條形圖`→`長條圖`。
- `資料模型實戰：從 MSSQL 設計到 .NET 8 WebAPI 實作（以刀具管理為例）.md`：索引建議 `ToolUsage.Date`／`Tool.Name` 對齊 2.1 DDL 的 `UsageDate`／`ToolName`；章節編號 2.4 後跳 2.6 改為 2.5；OpenTelemetry 範例 lambda 參數 `builder` 與外層區域變數同名（CS0136 編譯錯誤），改名 `tracing`。

## 疑似問題但未改

### 最嚴重

- `ASP.NET Core 後台查詢頁面教學.md`：**整份中文內文在位元組層級已毀損，無法審核也無法逐點修**。原檔為 Big5，曾被當成 UTF-8 做過一次有損轉換，Big5 lead byte（0xA1–0xBF 段）被丟棄只剩 trail byte（句尾的 `A` 是「，」的 trail byte、`C` 是「。」、`G` 是「：」）。已用 `od -c` 獨立驗證屬實。程式碼區塊（純 ASCII）完好，Bootstrap／Vue3／Dapper／SQL 範例檢查無誤。建議從原站 blog.hungwin.com.tw 重新取回原文並以 UTF-8 存檔。
- `ASP.NET Core 6 目錄.md`：六個章節連結全部指向 `#Chapter1`～`#Chapter6`，該檔內完全沒有對應 `<a name="ChapterN"/>`，所有連結皆為死連結；第六項標題「ASP.NET Core 如何使用 NLog 將 log 寫到檔案」與實際檔名 `ASP.NET Core NLog-如何使用 NLog 將 log 寫到檔案.md` 不符。下方時數表列的 15 個章節與上方目錄幾乎不重疊，多數在 repo 內沒有對應檔案。要修得改成相對檔案連結，屬改寫而非修錯。
- 前台會員四篇檔名冠「ASP.NET Core」，但內容從頭到尾是 **ASP.NET MVC 5 / .NET Framework**（`Web.config`、`System.Web.Configuration.WebConfigurationManager`、`Session[...]`、「ASP.NET Web 應用程式(.NET Framework)」範本）。屬分類／命名錯誤，改檔名超出授權範圍。

### 技術正確性存疑（reprint 為主，未動）

- `ASP.NET 9 OpenAPI 介紹與教學.md`：「產生多版本 OpenAPI 文件」那段在 `var app = builder.Build();` **之後**才呼叫 `builder.Services.AddOpenApiDocument(...)`，執行時拋 `InvalidOperationException`，且內文自己說「呼叫 Build() 之前」，自相矛盾；再者 `AddOpenApiDocument`／`PostProcess`／`DocumentName` 是 **NSwag** 的 API，不是文中列的 `Microsoft.AspNetCore.OpenApi`（後者為 `AddOpenApi(name, o => o.AddDocumentTransformer(...))`）。ReDoc 一次只吃一份 spec，`foreach` 逐版本設定只有最後一版生效；Scalar 2.x options 沒有 `RoutePrefix`。修正需整段重寫。
- `ASP.NET Core 使用 MassTransit…md`：`services.AddMassTransitHostedService();` 在 MassTransit v8（本文用 8.2.5）已移除，這行會編不過。
- `wdmis/資料模型實戰…md`：3.3 EF Core Power Tools 產出的 `Tool` 實體欄位（`Name`／`ToolType`／`PurchaseDate`／`IsActive`）與 2.1 的 `CREATE TABLE Tool`（`ToolCode`／`ToolName`／`Spec`／`CreatedAt`）完全不符，第 4、5 章 DTO／測試都沿用前者，要一致等於重寫兩章。另 `AddOpenTelemetryTracing` 在 OpenTelemetry 1.4+ 已淘汰（正解 `AddOpenTelemetry().WithTracing(...)`）；5.3 只裝 `xunit`，實務還缺 `Microsoft.NET.Test.Sdk`、`xunit.runner.visualstudio`。1.5 節後有一個空的 `## ` 標題。
- `ASP.NET Core 分散式Session – 使用 Redis.md`：內文寫「redis 安裝於 10.0.100.61」，但 appsettings 是 `10.0.100.60:6379`，且 .61 是應用程式主機，前後矛盾（正確應為 .60）。Dockerfile 範例：`RUN mkdir c:\app` 是 Windows 路徑卻用 Linux base image；每行後的 `# 註解` Dockerfile 不支援行內註解。
- `ASP.NET Core Hangfire 排程管理.md`：`HttpMethod method = HttpMethod.POST` — `System.Net.Http.HttpMethod` 是 class，不能當預設參數值（推測作者自訂同名 enum，文中未含）。安裝清單有 `Hangfire.MemoryStorage` 但範例用 `UseSqlServerStorage`。
- `ASP.NET Core Consul 教學.md`：`dotnet new webapi` 產生 Minimal API 範本，文中直接 `app.MapControllers()` 又建 Controller，缺 `builder.Services.AddControllers()`，照抄啟動失敗。Docker Hub `consul` 官方映像已停止維護，現行為 `hashicorp/consul`。
- `ASP.NET Core Quartz 介紹.md`：目錄 7 個錨點全部指向不存在的標題（全文除 `# 目錄` 外沒有任何標題，章節只用粗體）；整份換行在轉貼時被壓平（`Install-Package Quartz`／`quartz.config`／`quartz_jobs.xml`／C# 全黏成單行，`<` `_` `[` 被 escape），讀者無法直接複製。前後矛盾兩處：config 寫 `threadCount = 10` 但說明講「修改 = 2 中的 2」；輸出目錄一處寫 `d:\myservice`、後面指令一律 `d:\ScheduleJobService\`。
- `ASP.NET Core 如何套網頁設計樣版.md`：目錄 17 個連結全部指向不存在的 `#Step1`，且各級標題在轉貼時被壓平成內文；多處 HTML 標籤被轉檔吃掉造成語意殘缺（「搜尋程式碼 「」，找到完整區段的程式碼(從到結束的)」等），需回原文補標籤。
- `ASP.NET Core 網站生命週期.md`：目錄兩條連結都指向 `#Step1`，第二條應指向不存在的 `Step2`；表格中 `LogRequest` 的描述與上一列 `PostUpdateRequestCache` 完全重複。
- `ASP.NET Core Authentication系列（三）.md`：`options.Cookie.Expiration` 在 Cookie 驗證中不生效（應為 `options.ExpireTimeSpan`），屬 ASP.NET Core 已知陷阱。
- `ASP.NET Core NLog-依照Environment…md`：建立的檔名是小寫 `nlog.development.config`，程式 `$"nlog.{builder.Environment.EnvironmentName}.config"` 組出 `nlog.Development.config`，Linux 容器會找不到；`NLogBuilder.ConfigureNLog` 在 NLog.Web.AspNetCore 5.x 已標示過時。
- `LinePay.md`：`confirmUrl = "liff://app/YOUR_LIFF_ID"` 的 `liff://` scheme 已淘汰（現為 `https://liff.line.me/{LIFF_ID}`）；「設 `capture=false` 就會啟用內嵌結帳模式」的因果關係是錯的（`capture=false` 是僅授權、之後另行請款，與是否跳外部瀏覽器無關），整篇第 1 節前提站不住腳；Controller 內 `httpClient` 未宣告也未注入。
- `ASP.NET Core RDLC 報表設計.md`：`_northwindcontext`／`Reports.Const`／`DeliveryDataSet`／`BarcodeHelper` 都沒有宣告或 DI 說明，直接複製無法編譯；`Export` 用 `Directory.GetCurrentDirectory()`、`Print` 用 `AppContext.BaseDirectory` 不一致；`new BarcodeWriter { }` 在較新 ZXing.Net 是泛型型別。
- `ASP.NET Core KeyCloak 實作.md`：建構子 `Authenticate().Wait()` 同步阻塞非同步呼叫有死結風險且 token 永不更新；`admin-cli` 預設是 public client（無 secret），取 admin token 慣例打 `master` realm 而非目標 realm；`CreatedAtAction(..., new { userId = user.Id }, user)` 中 Keycloak 建立使用者不回傳 body，`user.Id` 為 null。
- `ASP.NET Core SFTP…md`：全檔 `catch (Exception ex) { throw ex; }` 清掉 stack trace；`sftp.ListDirectory()` 在 SSH.NET 2024.x 後回傳 `IEnumerable<ISftpFile>`，文中 `IEnumerable<SftpFile>` 只在舊版可編譯；`localFile = @"D:\your file*.txt"` 檔名含 `*` 無法開啟；`Move` 的 `isPosix` 註解敘述不精確；`[SSH.Net](http://ssh.net/)` 並非官方站。
- `NET 6 Dapper語法說明.md`：`conn.QueryFirstOrDefaultAsync<Student>(SQL.ToString(), trans)` 把 transaction 傳進 Dapper 第二參數位（`object param`）而非 `transaction`，應為 `(sql, null, trans)`。方法名一路寫 `ExcuteAsync`（Execute 拼錯）但前後一致且對應 GitHub 範例專案命名，改了會對不上。
- `NET 6 HttpClient單元測試範例.md`：開頭說「主要有兩種方式」，全文只講了 IHttpClientFactory 一種。
- `NET 6 EPPlus圖表範例.md`／`NET 6 EPPlus範例.md`：`LicenseContext` 設為 `"Commercial"`，非商業授權讀者應填 `"NonCommercial"`。
- `NET 6 Hangfire範例.md`：不帶 recurringJobId 的 `RecurringJob.AddOrUpdate` 多載在新版 Hangfire 已過時（以 .NET 6 當時版本而言正確）。
- `wdmis/CORS.md`：開頭說「.NET 8 中…可以直接在 `AddCors` 方法中定義策略，而不需要像以前一樣通過委派進行配置」，但底下範例本身就是用委派，且此寫法自 .NET Core 3.x 起未變，敘述與範例矛盾。
- `ASP.NET Core 客製化Model Validation…md`：`LocalizationValidationMetadataProvider` 缺 `System.Resources`／`System.Reflection`／`System.ComponentModel.DataAnnotations`／`Microsoft.AspNetCore.Mvc.ModelBinding.Validation` 等 using，class 大括號縮排異常。
- 前台會員系列：註冊篇 `DbDataAdapter adpt = new SqlDataAdapter();` 與其餘三篇不一致（可編譯）；修改個人資料篇 `data.form` 未預先宣告 `NewUserPwd`／`CheckUserPwd`，Vue 2 下非響應式；忘記密碼篇 `if (verify == "")` 判空應為 `string.IsNullOrEmpty`、`catch (Exception ex)` 的 `ex` 未使用、Web.config 的 `<appSettings>` 片段標成 ```` ```C# ```` 但內容是 XML、Gmail「允許低安全性應用程式」設定 Google 已於 2022 停用；四篇多處目錄錨點（`#Step1`～`#step18`）文中無對應 anchor；四篇均提到「文末有完整程式碼下載」但轉貼版本沒有該段落。

### 小疵（未動）

- `NET 6 Bogus範例.md`：註解「從 1-100 隨機取值」但程式是 `f.Random.Int(0, 100)`。
- `NET 6 Autofac範例.md`：「新增類別檔」段有一個空的清單項目（單獨 `- `）。
- `NET 6 EFCore語法說明.md`：標題「### 編輯WeatherForecastController檔案編輯」句尾多一個「編輯」。
- `NET 6 Automapper範例.md`：三筆 `DbModel` 的 `Id` 全填 1，疑似想寫 1/2/3。
- `NET 6 Telegram.Bot範例.md`：`new TelegramBotClient("前置作業給的機器人ID")` 參數其實是 bot token；「範例檔」標題後掛了網頁複製殘留的 `[#](...)` 錨點連結。
- `NET 6 swagger授權.md`：註解「加密的key，拿來比對jwt-token沒有」句子不完整；`AuthorizeCheckOperationFilter` 首行縮排異常；內文 `Authorize(Roles = “admin”)` 用全形彎引號。
- `wdmis/Serilog.md`：結構化日誌輸出區塊標成 ` ```yaml ` 但實際是純文字。
- `ASP.NET Core Quartz.NET 管理介面.md`：兩處把輪詢寫成 `Pulling`（標準術語為 `Polling`）。
- `ASP.NET Core Quartz 介紹.md`：`log4net.config` 區塊註解全為簡體（reprint 不做全文用語轉換）。
- `ASP.NET Core Authentication系列（二）.md`：「關鍵在於下面三行代碼」後列了 4 點；（四）「支持分佈部署」漏「式」字。
- `ASP.NET Core 中使用 HttpReports…md`（`kind: unknown`）：標題寫「配置 appsetting.json」但實際檔名是 `appsettings.json`（2 處）；「Transport -」後面是空的；`.AddSQLServerStorage()` 對應套件名大小寫（`HttpReports.SQLServer` vs 文中 `HttpReports.SqlServer`）無法確認。
- `ASP.NET RulesEngine 介紹.md`（`kind: unknown`）：JSON 設定檔含 `//` 註解（嚴格 JSON 不合法，走 Newtonsoft 可容忍）；JSON 多出情境說明沒提到的「週年慶消費」規則且其 `SuccessEvent`／`ErrorMessage` 是複製上一條；「範圍参数」是簡體殘留。
- `ASP.NET Core 呼叫 API 發生 CORS 錯誤.md`：`app.UseEndpoints();` 無參數呼叫無法編譯（示意性寫法）。
- `ASP.NET Core 使用 MassTransit…md`：`MessagePublishService4` 對 `OrderSubmitted` 連續 Publish 兩次；文章標題「ASP.NET 使用…」與檔名「ASP.NET Core …」不符。
- 前台會員註冊篇「泄漏」，繁中慣用「洩漏」（reprint 不做用語轉換）。

### 已驗證無誤（記錄備查）

- Quartz 兩篇的 cron 運算式全部正確：管理介面的 `0/30`／`0/52`／`0/13`／`0/20 * * * * ?` 皆為 6 欄位秒開頭，成果表格觸發秒數與運算式一致；「秒、分、時、日、月、星期、年」7 欄位描述符合規格；介紹篇 `0/3 * * * * ?` 對應「每 3 秒」正確。NuGet 套件名（Quartz、Quartz.Jobs、Quartz.Plugins、Topshelf、Topshelf.Log4Net、log4net）與 API 名（`IJobFactory.NewJob`、`Scheduler.JobFactory`、`WithCronSchedule`、`ISchedulerListener.SchedulerShuttingdown`、`XMLSchedulingDataProcessorPlugin`）查核無誤。
- `客製化Model Validation` 的 `GetMessageName` 型別比對順序正確：`DataTypeAttribute` 分支置於 `ValidationAttribute` 之前，`EmailAddress`／`Phone`／`Url`／`CreditCard`／`FileExtensions` 皆繼承 `DataTypeAttribute`，可正確落到 `_Invalid`。
- 全目錄無 `https:/` 少斜線的網址格式錯誤。
- 所有已修改檔案經 UTF-8 解碼驗證通過，未因編輯引入編碼損壞。

## 統計

審核 64 篇，修改 40 篇，共 132 處。
# devtools/ 逐篇審核報告

## 已修改

- `devtools/git/Git 安裝及配置SSH Key.md`：修正 `ssh-keygen` 指令被空白／連字號打斷（`ssh-keygen - t- rsa - b 2048` → `ssh-keygen -t rsa -b 2048`），共 2 處（內文與範例輸出）。
- `devtools/git/Git 安裝及配置SSH Key.md`：範例輸出「Your identification has been saved in ...id_dsa.」改為 `id_rsa`（前後矛盾，該段是 RSA 流程）。
- `devtools/git/Git 安裝及配置SSH Key.md`：多金鑰章節 config 的 `IdentityFile` 與前面 `ssh-keygen -f` 產生的檔名不符，`gitlab_id-rsa`／`github_id-rsa` 改為 `gitlab-rsa`／`github-rsa`。
- `devtools/git/Git 安裝及配置SSH Key.md`：錯字修正 —「看是是否有」→「看是否有」、「已存則跳過」→「已存在則跳過」、「SSH keys黨要存在哪裡」→「檔要存在哪裡」、「passpharse」→「passphrase」、「對應的的主機名稱」→「對應的主機名稱」（2 處）。
- `devtools/git/Git 安裝及配置SSH Key.md`：`` `id_rsa.pub' `` 反引號／單引號不成對，改為 `` `id_rsa.pub` ``。
- `devtools/git/Git 安裝及配置SSH Key.md`：簡繁殘留「文件夾中新建…的文件, 記事本打開」→「資料夾中新建…的檔案，用記事本開啟」。
- `devtools/git/Git Flow 語法大全.md`：第 9 節「停止使用 Git Flow」下的 `git flow init -d` 說明錯誤（`-d` 是以預設值初始化，不會停用 Git Flow），改為「如果你想以預設值重新初始化 Git Flow」。
- `devtools/git/Git Flow 語法大全.md`：空 commit 章節第 3 點標題「修改已有 commit 為空」與內文（再追加一個空 commit）不符，標題改為「再追加一個空 commit」。
- `devtools/git/Git 語法大全.md`：`git config`：git 配置。 後面接的一串亂碼 `vcr24f2erc...ilwoq` 刪除。
- `devtools/git/Git 流程規劃.md`：「中央服務器」→「中央伺服器」。
- `devtools/git/Git 從建立到上傳.md`：「默認的上傳位置」「默認分支」→「預設的上傳位置」「預設分支」（2 處）。
- `devtools/git/完整的 Git Flow + Semantic Release 整合流程.md`：3 個連結指向 `vscode-webview://...` 的內部 webview URL（外部完全無法開啟），改為純程式碼字樣 `` `.doc/CI-CD/...md` ``、`` `CLAUDE.md` ``、`` `README.md` ``。
- `devtools/git/30-days-git/10.md`：「commit 物件的的內容」→「的內容」。
- `devtools/git/30-days-git/26.md`：「本地追蹤分支的的版本線圖」→「的版本線圖」。
- `devtools/git/30-days-git/14.md`：「用指令列工具再輸入」→「在輸入」。
- `devtools/github-copilot/【GitHub Copilot】用 VS Code 解鎖 AI 寫扣超能力.md`：5 處 `(資料來源: [文字)](網址)` 的括號位置錯誤導致連結壞掉，修為 `(資料來源: [文字](網址))`。
- `devtools/github-copilot/【GitHub Copilot】用 VS Code 解鎖 AI 寫扣超能力.md`：「GitHut Copilot」→「GitHub Copilot」。
- `devtools/github-copilot/【GitHub Copilot】自動從 SQL 生成 UML.md`：同樣的 `[Shop Floor Management ER Diagram)](...)` 壞連結修正。
- `devtools/markdown/Markdown Cheatsheet 中文版.md`：目錄行 `[Lists](#lists)` 後多出的兩個反引號 `` `` `` 刪除。
- `devtools/markdown/Markdown Cheatsheet 中文版.md`：「要使文字段行」→「要使文字斷行」（2 處，程式碼示例與渲染結果各一）。
- `devtools/markdown/Markdown Cheatsheet 中文版.md`：「就為伴了典型的 GFM 斷行格式」→「就違反了」（2 處，原文為 contrary to）。
- `devtools/markdown/Markdown Cheatsheet 中文版.md`：mermaid 圓餅圖 `"Magnesium" : 10.01` 重複兩行，刪除重複的一行。
- `devtools/markdown/Markdown語法大全.md`：「産生斷行」→「產生斷行」（異體字）。
- `devtools/Mac 裝機必要.md`：「ForiClient」→「FortiClient」。
- `devtools/Mac 裝機必要.md`：「無蝦咪字體檔」→「無蝦米字根表」（連結指向 `liu7.cin`，是字根碼表不是字型檔）。
- `devtools/Mac 裝機必要.md`：「Coinfig Transform」→「Config Transform」（2 處）。
- `devtools/Mac 裝機必要.md`：「Viusal Studio」→「Visual Studio」（2 處）。
- `devtools/Mac 裝機必要.md`：「單元測試個離框架」→「隔離框架」、「在產建畫面 Sample 時」→「在建立畫面 Sample 時」。
- `devtools/Mac 裝機必要.md`：Tabnine 說明「AI 驅動的代碼補全工具，支持多種編程語言」→「程式碼補全工具，支援多種程式語言」；「使用滑鼠滾輪來移動文檔」→「移動文件」。
- `devtools/visual-studio/NET CLI 指令碼介紹.md`：7.2「查看項目依賴項樹」下的指令是 `dotnet list package --vulnerable`（列出有漏洞的套件，非相依樹），改為 `dotnet list package --include-transitive`。

## 疑似問題但未改

- `devtools/visual-studio/NET CLI 指令碼介紹.md`：全文為中國大陸用語（項目／命令／應用程序／構建／創建／文件／類庫／包／調試／卸載／文檔／全局／模板），不是零星殘留而是通篇文體。逐詞轉換等同重寫，超出「外科手術式修改」範圍，留待你決定是否整篇正名。同理 `devtools/git/Git 流程規劃.md` 亦大量使用「質量／創建／登錄／用戶／構建／文檔」，只改了最刺眼的「服務器」。
- `devtools/visual-studio/NET CLI 指令碼介紹.md`：`dotnet new --list` 出現兩次（2.1、7.1）。新版 SDK 建議寫法為 `dotnet new list`，`--list` 已 deprecated 但仍可用，且原文未標註 SDK 版本，故未動。
- `devtools/git/完整的 Git Flow + Semantic Release 整合流程.md`：`.releaserc` 範例是 JSON，卻在值後面加了 `// 正式版本` 之類的行內註解。標準 JSON 不允許註解，直接複製會解析失敗。但註解顯然是刻意加的說明，刪掉會損失資訊，未改。
- `devtools/git/Git Flow 語法大全.md`：`git flow feature pull origin <feature-name>` — 此子指令在 gitflow-avh 已被 `git flow feature track` 取代（部分版本已移除）。無法確認你用的版本，未改。
- `devtools/git/Git Flow 語法大全.md`：rebase 章節寫「它會執行 `git fetch origin` + `git rebase origin/develop`」。實際 `git flow feature rebase` 只 rebase 到本地 `develop`，不會自動 fetch。屬於敘述近似而非指令錯誤，未改。
- `devtools/git/Git 從建立到上傳.md`：第 5 步已用 `git branch -M main` 改名，第 7 步卻先給 `git push -u origin master`，兩步照做會失敗。因第 5、6 步標了「(選擇性)」，順序意圖不明確，未改。
- `devtools/markdown/HackMD 特殊 Markdown 語法.md`：Gist 範例第二行 `{%gist github後的連結/"%}` 尾端多一個 `"`，疑似誤打，但不確定是否 HackMD 某種寫法，未改。
- `devtools/markdown/Markdown語法大全.md`：開頭用 `[Toc]` 而非慣用的 `[TOC]`。HackMD 實測大小寫皆可，且屬轉貼原文，未改。
- `devtools/Mac 裝機必要.md`：連結時效問題 —「Visual Studio 2022 (Mac)」已於 2024 停止支援、Atom 已停止開發、`[Rider]` 連到 JetBrains 首頁而非 Rider 產品頁、`.NET Core Extension Pack` 在 VS Code 清單中重複列出（第 3 與第 7 項）。屬內容更新而非錯誤，未改。
- `devtools/Mac 裝機必要.md`：Visual Studio 延伸模組清單編號連續出現三次「22.」，NuGet 清單有一行「15 單元測試相關套件：」漏了句點。Markdown 渲染時序號會自動重排，屬格式風格，未改。
- `devtools/visual-studio/使用 Visual Studio 2022 ... .editorconfig ....md`：`.editorconfig` 的副檔名清單中 `txt` 出現 3 次、`asciidoc` 出現 2 次。重複無害且為保哥原文，未改。
- `devtools/git/30-days-git/` 30 篇：依指示只做快速掃描（重複字、壞掉的 URL、簡繁殘留、可疑指令）。未發現簡繁混雜，僅 3 處錯字已修；`git sts`／`git statsu`／`git core\git-stash` 經確認分別是自訂 alias、刻意的打錯字示範、Git 原始輸出，皆非錯誤。

## 統計

審核 51 篇，修改 15 篇，共 34 處
# dotnet/ 審核報告（不含 aspnet-core）

## 已修改

### csharp/
- `csharp/C# 集合, List 取交集、差集、聯集的方法.md`（original）：聯集註解的輸出結果從 `1, 2, 3, 4, 5, 6, 7, 8` 改為 `1, 2, 3, 4, 5, 6, 8`（兩個 List 裡都沒有 7）；程式碼註解的簡體殘留（并集／输出／两个／与／合并／一个）改為繁體。
- `csharp/ASP.NET await 與 wait 的差異.md`（original）：`Task / Task / void` → `Task / Task<T> / void`（`<T>` 被 HTML 吃掉）；第二個劍橋字典連結標題與網址都是 `wait`，但引的是 await 的釋義 → 改為 `await`；`awiat` → `await`；`retun` → `return`；`顯在是使用` → `顯然是使用`。
- `csharp/CORS 介紹與設定方式.md`（original）：中國用語改台灣用語 — 服務器→伺服器（4）、緩存→快取（3）、訪問→存取（2）、端口（port）→連接埠（port）。
- `csharp/AutoMapper —— 類別轉換超省力.md`（original）：「忽略指定的欄位」的程式碼區塊結尾多一個 `}`，移除。
- `csharp/CSharp Coding Conventions.md`（reprint）：`/ CORRECT` → `// CORRECT`（2 處，缺一個斜線）；`“Entites”` → `“Entities”`；`private int _isRunning = false;` / `private int _run = false;` → `bool`（int 指派 bool 無法編譯）；`public const string MAXIMUM_FILE_SIZE = 1024;` → `const int`；`abbrevation` → `abbreviation`。
- `csharp/C# 中的 HTTPClient — 入門指南.md`（reprint）：`pplication/json` → `application/json`（2 處）；`將所有內容髮送到目標後` → `發送`（简繁轉換錯字）。
- `csharp/C# 正則表達式：從零到英雄指南.md`（reprint）：副標題的 regex `^(?=.*[az])(?=.*[AZ])(?=.*).*` 字元類的 `-` 與 `\d` 在轉貼時遺失 → 依內文實際範例修正為 `^(?=.*[a-z])(?=.*[A-Z])(?=.*\d).*`；`開髮乾淨` → `開發乾淨`（2 處）；`演示瞭如何` → `演示了如何`（2 處）。
- `csharp/Using jQuery Unobtrusive AJAX in ASP.NET Core Razor Pages.md`（reprint）：template literal 少了 `$` → `` `Status: ${xhr.status}, Status Text: ${xhr.statusText}` ``。
- `csharp/多奇數位 C# 程式碼撰寫規範 (C# Coding Guideline).md`（original）：5.2 節「型別參數 (Type Parameter)：適用於**命名空間**」→「適用於型別參數」；`訪問外部變數` → `存取外部變數`（3 處，同一份文件他處都用「存取」）。
- `csharp/深入Dapper.NET源碼.md`（reprint）：`Visaul Studio` → `Visual Studio`；`Entity Framwork` → `Entity Framework`；`DbCommand的Paramter` → `Parameter`；`Dapper DynamicParamter` → `DynamicParameter`。

### csharp/菜雞與物件導向/（全 reprint，17 篇）
- `(1) 類別、物件.md`：`Dog` 宣告 `public string color;` 但下方用 `afu.Color`，大小寫不一致無法編譯 → 依同檔慣例改為 `Color`。
- `(3) 封裝.md`：`攔位` → `欄位`。
- `(5) 多型.md`：`徵人啟示` → `徵人啟事`；`如果一來` → `如此一來`。
- `(7) 介面.md`：`徵人啟示` → `徵人啟事`。
- `(8) 內聚、耦合.md`：「就是就是把用到的東西」刪去贅字。
- `(9) SOLID.md`：`一但` → `一旦`。
- `(10) 單一職責原則.md`：「就只是盤散沙」→「一盤散沙」。
- `(12) 里氏替換原則.md`：`參考至` → `參考自`。
- `(13) 介面隔離原則.md`：`SOILD` → `SOLID`；「決定先用介面先規定好」刪贅字。
- `(15) 最少知識原則.md`：兩個壞掉的站內連結 `post/202007-oo-7-interface`、`post/202007-oo-3-encapsulation` → `post/2020/07/...`（全系列其餘 60+ 處都是後者格式）；`燈炮` → `燈泡`。
- `(Ex1) 小結.md`：`post/2021/oo-ex1-end2020` 缺月份 → `post/2021/01/oo-ex1-end2020`；兩處「的」→「了」。

### entity-framework/
- `EF Core Migrations 完整教學手冊.md`（original）：7.1 CRUD 範例用 `new AppDbContext()`，但 `AppDbContext` 只有帶 `DbContextOptions` 的建構式，無法編譯 → 補上無參數建構式；示範 Mermaid 用法的巢狀 code fence 少了內層的收尾 ```。
- `EntityFramework Core DB Migrations.md`（original）：`接口` → `介面`（2 處）。

### testing/
- `C# 單元測試教學.md`（original）：`Add_InputTwoInreger_ReturnSum` → `Integer`；`string.StartWith` / `Assert.StartWith` → `StartsWith`（.NET 與 xUnit 的實際方法名）；`節錄至` → `節錄自`；「依照網頁的標題來是否搜尋成功」補上「判斷」；dotCover 指令的參數被 Medium 轉成 em dash（`— output=`）→ `--output=` 等，並把 4 行 `dotcover cover` 指令的彎引號 `”` 改回 `"`（否則指令貼上不能用）；**技術錯誤**：「xUnit 使用建構、解構⋯⋯不會每個測試都重一次初始化⋯⋯初始化 → 跑完所有測試 → 清除狀態」與下一節「xUnit 在每個測試中分別使用新的 Instance」自相矛盾且說反了 → 改為每個測試建立新 Instance、建構式→該筆測試→Dispose。

### web-api/
- `ASP.NET Core SwaggerAPI說明文件.md`（reprint）：`Configure` 方法的程式碼區塊 `if (env.IsDevelopment())` 沒有收尾 `}`，且 `UseSwagger()`、`UseSwaggerUI()`、`UseHttpsRedirection()`、`UseRouting()`、`UseAuthorization()` 五行全缺分號 → 補齊。
- `ASP.NET Core Web Api實作JWT驗證筆記.md`（original）：XML 註解 `<param name="issur">` 與實際參數 `issuer` 不符；`Htpp請求` → `Http請求`；`Starup.cs` → `Startup.cs`；`需要再專案目錄內` → `在`；`否則會再請求進來時` → `在`；「避免Token重複在被套用」刪去多餘的「在」。
- `ECFIT API 範例.md`（original）：「取得訂單資訊，包含開立時的商品資料與折讓紀錄**)**」多出一個沒有配對的右括號（2 處）。
- `ASP.NET Core 6 Web API 進行 JWT 令牌身份驗證.md`（reprint）：步驟 3 `Product` 類別的 namespace 缺結尾 `}`。
- `JWT Token Authentication And Authorizations In .Net Core 6.0 Web API.md`（reprint）：`AddJWTTokenServices(IServiceCollection Services, ...)` 缺 `this`，但後文當 extension method 呼叫，編譯不過 → 補 `this`；`return BadRequest($ "wrong password");` → `$"wrong password"`；內文 `AddSecurityDefination()` → `AddSecurityDefinition()`；`JWT Hepler` → `JWT Helper`。

### line-bot/（全 reprint）
- `00. 如何申請LINE Bot.md`：「**們**待會需要的是」→「我們待會」。
- `basic/Lab 02 _ 推送LINE Template Messages.md`：`Tempalte` → `Template`（4 個出現位置）。
- `webhook/Lab 11 _ 如何建立可Echo的 LINE Bot.md`：`只少五支` → `至少五支`；`endppint` → `endpoint`。
- `liff/Lab 21_ 建立第一個LIFF應用.md`：`在本基上安裝` → `本機`；`保留的 LiffAddID` → `LiffAppID`（與上一句 placeholder 不一致）。
- `CLI/Lab 31_使用CLI來發送新的Channel Access Token(LINE Bot).md`：`Channel Access Tokem` → `Token`。

### 菜雞新訓記/（全 reprint，8 篇）
- `(1) 使用 Git 來進行版本控制吧.md`（14 處）：`SVM` → `SVN`；`Git Igonre` / `.gitigonre` → `Ignore` / `.gitignore`；`Commit Messaage` → `Message`；`git -add A.txt` → `git add A.txt`；`got log` → `git log`；`Stach` / `stach` → `Stash` / `stash`（4 處）；`add -a` → `add -A`；rebase 段落「將 Branch-X 併入 master」→「Branch-Y」（與範例指令 `git rebase Branch-Y` 一致）；`擷取（Fatch）` → `Fetch`；連結文字缺左書名號。
- `(2) 認識 Api & 使用 .net Core ...md`：「接續在往指後面」→「網址後面」；`[FromRpute]` → `[FromRoute]`。
- `(3) 使用 Dapper ...md`：「從資料庫裏面撈資料庫」→「撈資料」；`sql += " And Cost <= @Cost "` 補結尾分號。
- `(4) 使用 Swagger ...md`：小結的 `Swashbuckle.Core` → `Swashbuckle.AspNetCore`（與內文安裝步驟一致，`.Core` 是舊 ASP.NET 版套件）；「也就來說」→「也就是說」。
- `(5) 使用 三層式架構 ...md`：`CardRepository.GetList` 裡 `parameter.Add("Name", $"%{condition.MaxHealth}%")` → `condition.Name`（變數用錯）；「如果一來」→「如此一來」；「增修查改」→「增刪查改」；「或則是」→「或者是」。
- `(6) 使用 依賴注入 ...md`（7 處）：`SOILD` → `SOLID`；`HttpCilent` → `HttpClient`；延伸閱讀連結尾端多出的 `>)` 移除；「DI 容易」→「DI 容器」；`// Repsitory` → `// Repository`（2 處）；「這個多個對象」→「這麼多個對象」。
- `(7) 使用 Fluent Validation ...md`：`EquipmentValidator : AbstractValidator<Card>` → `AbstractValidator<Equipment>`（示範 SetValidator 時泛型指錯類別）；「把傳入參數的分離出去」刪贅字；注音符號 `ㄧ` → `一`。

---

## 疑似問題但未改

### 技術／語意疑點（建議人工定奪）
- `csharp/菜雞與物件導向/(15) 最少知識原則.md`：「為了方便管理和降低複雜性，減少臭味出現的機率，我們的目標就是追求**耦合**。」語意顯然相反，應為「降低耦合」或「追求低耦合」。**本次找到最實質的語意錯誤**，但無法確定作者原句用詞，未動。
- `testing/C# 單元測試教學.md`：「這樣子程式碼覆蓋率就是 75%，因為 5 個 case 中只測試了 3 個 case」——3/5 = 60%，數字與理由對不上。但覆蓋率是工具依「行」計算，截圖不可考，未動。
- `testing/C# 單元測試教學.md`：「TestData 內提供 2 種給予測資的方式 AddData 與 **AddData2**」——xUnit `TheoryData` 只有 `AddData`，`AddData2` 不存在；可能是指截圖裡的兩個屬性，未動。
- `csharp/C# 正則表達式：從零到英雄指南.md`：三個 lookahead/lookbehind 範例的註解輸出與實際比對結果對不上（`\ba(?:\w+)(?= a\b)` 對 "I want to buy a car and a bike" 實際會匹配 `and` 而非註解寫的 `car`；lookbehind 那題實際會匹配到兩個 `kg`）。屬原作者的錯誤，改動幅度大，未動。
- `csharp/C# 中的 HTTPClient — 入門指南.md`：「HTTPClient**自動手動**解壓響應內容」自相矛盾（機器翻譯痕跡）；另有「該財產的價值」（property 誤譯為財產）。屬譯文品質，reprint 未動。
- `csharp/ASP.NET await 與 wait 的差異.md`：「會進入到封鎖狀態下，持續地獲得非同步工作已經完成了」、「才能夠繼續**網頁**執行下去」、「可以啟動失敗錯誤紀錄」三句都疑似漏字或誤字，但無法確定原意，未動。
- `entity-framework/EntityFramework Core DB Migrations.md`：`CreateDbContext` 用到 `Directory.GetCurrentDirectory()` 但列出的 using 沒有 `System.IO`（新版 ImplicitUsings 下沒問題），未動。
- `csharp/菜雞與物件導向/(4) 繼承.md`：重構後 `MagicCard : Card` 仍保留 `public int Cost;`，與基底類別重複（CS0108 警告），與同段「將重複的部份移除」的敘述矛盾。刪行屬結構改動，未動。
- `csharp/菜雞與物件導向/(5) 多型.md`：`Hua`／`Ming` 覆寫父類方法時未用 `virtual`/`override`，實際是方法隱藏，執行結果與註解不符。屬教學取捨（`override` 留到第 6 篇），未動。
- `csharp/菜雞與物件導向/(1) 類別、物件.md`：`public void Eat(IFood food) { /* ... */ };` 類別成員後多一分號（CS1519），屬示意片段，未動。
- `菜雞新訓記/(6) 依賴注入`：`IProgrammer.Programming()` 宣告為 `void`，但呼叫端 `var product = programmer.Programming(); return product;` 型別對不上。要修得改介面與實作兩處，屬改寫範例，未動。
- `菜雞新訓記/(2) Web Api`：「501: 未實作，通常對一個只提供 GET 的 URL 打 POST 就會看到」（實務多回 405）、「303: 臨時轉址」（一般歸 302）；另「建立一個 static 並且為空的 `IEnumerable<Card>` 的私有成員」與程式碼的 `List<Card>` 不符。屬作者敘述，未動。
- `菜雞新訓記/(3) Dapper`：內文說 `Create()` 用 `Query<int>` 但程式碼是 `QueryFirstOrDefault<int>`；「C# 8.0 using 不需要大括弧」的範例其實是傳統 using 陳述式。敘述不精確但程式碼合法，未動。
- `line-bot/basic/Lab 03_如何發送ImageMap訊息.md`：`ImagemapArea(500, 0, 1040, 1040)` 的 x=500 加 width=1040 會超出 baseSize 1040（第二塊應為寬 540）。不確定 LINE 端是否自動裁切，未動。
- `line-bot/webhook/Lab 13`：image 分支內 `responseMsg = $"你說了: {LineEvent.message.text}";` 是複製殘留（下一行立刻覆寫，且 image 訊息沒有 text）。刪行等於改作者程式碼，未動。
- `web-api/JWT Token Authentication ... .Net Core 6.0 Web API.md`：`UserTokens.Validaty` 拼錯（應為 Validity），但宣告與使用處一致，改動會擴散；`ValidateLifetime = bindJwtSettings.RequireExpirationTime` 疑似應為 `.ValidateLifetime`。皆未動。

### 用語／格式疑點
- `csharp/AutoMapper —— 類別轉換超省力.md`：front matter 標為 `kind: original`，但全文連結指向 igouist.github.io、文末有「下週見」與該作者的其他文章清單，實際應為 reprint。依規定未動 front matter，僅記錄。
- `csharp/WinForms APP 程式安裝檔.md`：「下載連結：VS2017, VS2019，VS2022」三個下載連結的超連結在轉貼時遺失，只剩文字。無從補上正確網址，未動。
- `csharp/Using jQuery Unobtrusive AJAX in ASP.NET Core Razor Pages.md`：全部 code block 在抓取時換行被壓成一行（`@page@model IndexModel<form method="post"...`），可讀性差。要修等於重排全部程式碼，屬重寫，未動。
- `csharp/C# 中的 HTTPClient — 入門指南.md`、`C# 正則表達式`、`ASP.NET Core 6 Web API JWT`、`testing/單元測試.md`：整篇是簡→繁機器轉換的中國用語（數據、響應、代碼、字符串、項目⋯⋯）。依 reprint 規則不做全文用語轉換。
- `testing/單元測試.md`（reprint）：`Junit` 應為 `JUnit`（大小寫），屬專有名詞細節，未動。
- `web-api/ECFIT API 範例.md`：整份是廠商 API 規格轉貼（訪問頻率、代收金額等欄位語意無法驗證），僅做結構與括號檢查。
- `web-api/ASP.NET Core Web Api實作JWT驗證筆記.md`：`NLog.Web.AspNetCore` 的 NuGet 連結指向 `4.13.0-readme-preview`，但安裝指令是 `--version 4.12.0`；另 `Visual Code` 應為 `Visual Studio Code`。皆屬小瑕疵，未動。
- `entity-framework/EF Core Migrations 完整教學手冊.md`：目錄列出 15 節但正文只寫到第 7 節（作者自註「後續章節可依需求補齊」），非錯誤。
- `csharp/多奇數位 C# 程式碼撰寫規範.md`：4.1 與 4.4 兩個小節都叫「換行」，4.4 實際講的是「保留單行」。屬標題命名，未動。
- `line-bot/basic/Lab 05`：程式碼小節標題寫「撰寫程式碼發送 Flex Message」，但本 Lab 是 QuickReply（從 Lab 04 複製殘留）。屬作者標題用語，未動。
- `line-bot/basic/Lab 02`：分段示範中重複宣告 `var Actions`、`var Column`，照文貼進同一個 Main 會 CS0128。屬分段教學的既有寫法，未動。
- `line-bot/webhook/Lab 11`：placeholder 字串 `"___Repleace_it_with_your_Channel_Access_Token___"` 拼錯，但這是範本產生的實際字串，改了會對不上，未動。
- `line-bot/liff/Lab 21`：「安裝 liff 範本**特見**」語意不通（推測「套件」），無法確定原字，未動。
- `line-bot/00. 如何申請LINE Bot.md`：developers.line.biz 的連結文字與實際 href 不符（href 指向 blogger 編輯頁），原文即有的錯誤外部連結，未動。
- `菜雞新訓記/(5) 三層式架構`：參考連結 `...-pattern-smaple`（sample 拼錯）是 GitHub repo 的實際網址，改了會壞連結，未動。
- 全部檔案的相對路徑連結：檢查後無指向不存在檔案的站內連結；全部外部網址格式正確（無 `https:/` 少斜線的情況）。

---

## 統計

審核 59 篇，修改 41 篇，共 115 處
# infra/ 與 ai/ 文件審核報告

## 已修改

- `ai/Claude Code 核心概念介紹.md`：子代理路徑由 `.claude/agents/{name}/AGENT.md` 更正為 `.claude/agents/{name}.md`。
- `ai/Graphify + Claude Code 的實戰安裝手冊.md`：`.mcp.json` 範例缺少必要的 `mcpServers` 最外層鍵，補上並重排縮排。
- `ai/用 .NET 驅動 AI Agent 到 MCP 實踐.md`：標題 `用. NET` → `用 .NET`；`dotnet add package "Microsoft.SemanticKernel`（兩行）多餘的引號移除；串流對話範例結尾多出的孤兒 `}` 移除；「簡單來說，，」重複逗號；「前題」→「前提」。
- `infra/docker/Docker 語法.md`：`responsitory` → `repository`（2 處）；`conainter` → `container`。
- `infra/docker/第三章 安裝MSSQL.md`：`docer rm` → `docker rm`；「連線阜號」→「連線埠號」；`containder` → `container`。
- `infra/docker/第四章 安裝MySQL.md`：`mysql -h localhost -p 3306 -u root -p root` 的 `-p` 重複誤用，改為 `-P 3306 ... -p`；補上缺漏的右括號。
- `infra/docker/第五章 安裝Redis.md`：「啓動」→「啟動」（2 處）。
- `infra/docker/第六章 安裝RabbitMQ.md`：參數說明的 `–name`（全形連字號）→ `--name`（2 處）。
- `infra/docker/第八章 安裝SonarQube.md`：`vulenrability` → `vulnerability`；`**–name**` → `**--name**`。
- `infra/docker/第九章 安裝datalust.Seq.md`：`volumn mount` → `volume mount`；`[部署總覽]("https://…")` 網址多餘引號移除；`[C#：NLog…]([https://…)` 多餘 `[` 移除。
- `infra/docker/第十章 安裝Redmine.md`：`(Dokcer)` → `(Docker)`。
- `infra/docker/第十一章 安裝 VtigerCRM.md`：疑難排解表中「套用第五章補丁」→「第五節補丁」（本章第五節，非第五章）。
- `infra/docker/第十三章 安裝 Qdrant.md`：說明文字的路徑 `C:/qdrant_data` 與上方指令 `E:/docker/qdrant_data` 不一致，改為一致。
- `infra/docker/第十四章 安裝 MongoDB.md`：aggregate 範例中 `\"$group\"` 的跳脫引號改為正常的 `"$group"`。
- `infra/iis-windows/IIS 不停機更版設置.md`：「如何部屬」→「部署」；「寫死再程式裡面」→「寫死在」；「這邊文章」→「這篇文章」。
- `infra/iis-windows/IIS - 如何解決網站第一個請求 Request 特別慢.md`：`Internal Information Service` → `Internet Information Services`；`preloadEnable` → `preloadEnabled`（IIS 實際屬性名）。
- `infra/iis-windows/IIS - 如何檢查安裝在 IIS 上的 .NET Core Hosting Bundle 版本.md`：`dotnet --version --list-runtime` → `dotnet --list-runtimes`；`regeidt.exe` → `regedit.exe`；「登陸檔」→「登錄檔」（3 處）；「經查需要查詢了話」→「經常需要查詢的話」。
- `infra/iis-windows/IIS - Log Paser Studio 分析 IIS W3C Log.md`：標題 `Log Paser Studio` → `Log Parser Studio`（檔名未動）。
- `infra/iis-windows/如何設定 ASP.NET CORE 網站應用程式持續執行在 IIS 上.md`：Server Manager 選單 `Manager > Add Roles and Features` → `Manage > …`。
- `infra/azure/Azure AD Authentication.md`：權限名稱 `opendid` → `openid`。
- `infra/azure/Azure Blob Storage.md`：`CURD` → `CRUD`。
- `infra/azure/Azure DevOps 持續整合(CI).md`：`(Premissions)` → `(Permissions)`；「在將 xxxxxxx.nupkg」→「再將」。
- `infra/ci-cd/開源程式碼檢測平台 SonarQube.md`：`Security Hotspotsy` → `Security Hotspots`；「會在再下一次分析」→「會在下一次分析」。
- `infra/tools/FileZilla Server 安裝教學.md`：`Vitual path` → `Virtual path`（同文後段拼寫正確）。

## 疑似問題但未改

- `infra/azure/Azure Blob Storage.md`（original）：**結構嚴重損壞**。第 11 行目錄的 markdown 連結全被跳脫（`\[Blob 檔案架構]\(#…)`）；第 13、26、78 行把好幾個章節與整段 C# 程式碼壓成單行純文字，`## 建立儲存體帳戶` 標題被塞在句子中間；圖片連結全部指向 `/Users/kerry/Library/CloudStorage/…` 的本機絕對路徑，讀者端必然壞掉。修復等同重寫整篇，超出外科手術式修改範圍，建議另開工作處理。
- `infra/docker/Docker 中部署 .NET 8 Web App 並支援 HTTPS.md`（original）：`docker-compose.yml` 只有 `image:` 沒有 `build:`，但文中要求執行 `docker-compose up --build -d`——沒有 build context 時 `--build` 不會建置，映像必須事先存在。要補 `build:` 需推測 context 與 Dockerfile 路徑，故未動。
- `infra/docker/第二章 docker-compose 教學.md`（original）：列出的 `docker-compose prune` 並非真實指令（Compose 沒有 prune 子命令，對應的是 `docker system prune`）。刪除整節屬於刪內容，未動。
- `infra/docker/第四章 安裝MySQL.md`（reprint）：後半段參數說明與指令不一致——指令用 `--name MYSQL8`、密碼 `!QAZ2wsx`，說明卻寫「指定容器為 sql2」、`MYSQL_ROOT_PASSWORD=Dev127336`，且 `docker rm sql2`。屬原作者的前後矛盾，reprint 不做用語與內容調整。
- `infra/docker/第三章 安裝MSSQL.md`（reprint）：(1)「刪除Container」段落寫「**建立**Container先停止運行中的Container」，語意上應為「刪除」；(2) 登入管理介面寫 `http://localhost:1433/`，但 SQL Server 走 TDS 非 HTTP，該行實際只是帳密說明。兩處皆為原作者敘述，未改。
- `infra/docker/第五章 安裝Redis.md`（reprint）：Python 範例註解「#輸入key值來取得剛」句子被截斷；Redis Stack 的 RedisGraph 模組已於 2023 年停止維護，新版 redis-stack-server 不再包含。
- `infra/docker/第六章 安裝RabbitMQ.md`（reprint）：第 83 行圖片語法缺少開頭的 `[`（`![img](url)](url)`），依規定不動圖片連結。
- `infra/docker/第七章 安裝Nomad.md`（original）：docker-compose 的 nomad-client 以 `NOMAD_SERVERS` 環境變數指定 server，Nomad agent 並不讀這個變數（一般用 `-servers` 旗標或 client 區塊設定），但無法確認該映像是否有 entrypoint 轉換，故未改。
- `infra/docker/第十二章 安裝KeyCloak.md`（original）：`KEYCLOAK_ADMIN` / `KEYCLOAK_ADMIN_PASSWORD` 在 Keycloak 26 之後已改為 `KC_BOOTSTRAP_ADMIN_USERNAME` / `KC_BOOTSTRAP_ADMIN_PASSWORD`，搭配文中的 `:latest` tag 會失效。屬版本演進而非撰寫錯誤，未動。
- `infra/docker/第十四章 安裝 MongoDB.md`（original）：Windows 指令的說明列有「請將 `你的帳號` 換成自己的 Windows 使用者名稱」「路徑格式為 `C:\Users\...`」，但上方指令用的是 `E:\Database\mongo-data`，沒有 `你的帳號` 佔位符——像是從別處複製殘留。刪除屬刪內容，未動。
- `ai/Claude Code 核心概念介紹.md`（original）：表格中動態內容語法寫成 `` `!`command`` ``，巢狀反引號在 markdown 會渲染錯誤（正確寫法為 `` !`command` ``）。屬渲染而非技術內容，未動。
- 全域用語：多篇（含 original）大量使用「端口」「文件」「代碼」「質量」「映像/鏡像」「數據」「後台」「緩存」「日誌」「進程」等大陸用語。這是全文用語風格而非簡繁殘留字，逐字轉換會變成重寫，依「外科手術式修改」原則未處理；若要統一，建議另開一次專門的用語校正。
- 連結檢查：全數 .md 未發現 `https:/` 少斜線之類的格式錯誤；`images/` 相對路徑（mssql-*.png、vtiger-zhtw-*.png、windows_certificate_manager-*.jpg）皆對應到同目錄的 images 資料夾。惟 `infra/docker/第三章 安裝MSSQL.md` 第 157 行仍指向原站 dotblogsfile.blob.core.windows.net 的外部圖，未動。

## 統計

審核 36 篇，修改 23 篇，共 41 處
# management/ 內容審核報告

審核範圍：`management/` 及所有子目錄的 .md（略過 README.md），共 54 篇。

---

## 已修改

### redmine-mis 第1–15章（全為 reprint，僅修客觀錯誤）
- redmine-mis/第1章 - [MIS及專案管理]中小企業MIS的鳥事.md：刪重複「去管理去管理」；「甚麼才事鳥事」→「甚麼才是鳥事」
- redmine-mis/第2章 - [MIS及專案管理]專案管理的概念及MIS應用.md：「本系統文章」→「本系列文章」
- redmine-mis/第3章 - [MIS及專案管理]管理工具的選擇.md：刪重複「從幾個面向去幾個面向去」；「事務跟蹤系管理統」→「事務跟蹤管理系統」；產品名 Trella → Trello（2 處）
- redmine-mis/第4章 - [Redmine]Redmine的安裝及設定.md：Dokcer → Docker；「在頁按面[Access Redmine]」→「在頁面按」；Redmine 英文介面選單 [Administrator] → [Administration]
- redmine-mis/第5章 - [Redmine]Redime系統邏輯說明.md：「Tick List」→「Ticket List」；「議題的狀題」→「議題的狀態」；「議題的追狀態清單」→「議題的狀態清單」
- redmine-mis/第6章 - [Redmine]自行建立及維護表單.md：「專案資本資料」→「基本資料」；「[追蹤邊謙清單]」→「[追蹤標籤清單]」
- redmine-mis/第7章 - [Redmine]專案版面的規劃.md：「上層方類」→「上層分類」；「本身否有建立Issue」→「本身是否有」
- redmine-mis/第8章 - [日常管理]AR管理.md：EIP Potral → Portal；「自怨自哀」→「自怨自艾」；「AR負責必須登入」→「AR負責人」；「最為這個AR」→「作為」；「自己配指派」→「被指派」；「被分配者」→「被分派者」（對齊 Redmine 欄位名，同章其他段落即用此名）
- redmine-mis/第9章 - [日常管理]資訊服務申請.md：「服務順訓」→「服務順序」；欄位「(10)需求摘要說明、」去多餘頓號；「破壞SPO」→「破壞SOP」
- redmine-mis/第10章 - [日常管理]MIS部門週會工作進度追蹤.md：「該筆Kicket」→「Ticket」；欄位多餘頓號；設定路徑 [自訂欄位清單] → [追蹤標籤清單]；「本此管理需求」→「本次」；「登入前後(MIS群組)」→「登入後」；「[完成日期】」括號不對稱；「近來編輯…有就是」→「進來編輯…也就是」；「惡性循!」→「惡性循環!」
- redmine-mis/第12章 - [日常管理]機房工作日誌.md：「沒次建立」→「每次」；模組清單「(X)文件(X)檔案」拆行；「[自定欄位]」→「[自訂欄位]」；「重點再審核」→「重點在審核」
- redmine-mis/第13章 - [日常管理]MIS部門耗用工時及工作進度檢討.md：甘特圖段刪重複動詞「可了解從甘特圖中了解」；行事曆段誤植成甘特圖敘述，改為「可從行事曆中了解…」
- redmine-mis/第14章 - [日常管理]MIS文件知識庫.md：「沒用的的資我估計」→「沒用的資料我估計」；「有權限的人員的去追查」去贅字；「不是機密的的行政申請」去疊字
- redmine-mis/第15章 - [日常管理]整理及管理分享.md：「在原本在一些文章」去重複「在」；「本系統文章」→「本系列文章」

### redmine-mis 第16–30章（全為 reprint，僅修客觀錯誤）
- redmine-mis/第16章 - [異常管理]使用者問題回報系統.md：`Isuue` → `Issue`；「協助排處處理」→「協助排除處理」
- redmine-mis/第17章 - [異常管理]資安事件及異常紀錄.md：「依據通安全管理法」→「依據資通安全管理法」
- redmine-mis/第18章 - [異常管理]整理及管理分享.md：「過程過程」去重字；「請再回過程看」→「回過頭看」；「由擬自行決定」→「由你自行決定」；「如鶴設計」→「如何設計」
- redmine-mis/第21章 - [變革管理]MIS的新專案管理：規劃階段.md：「(圖˙07、08)」移除誤植間隔號
- redmine-mis/第22章 - [變革管理]MIS的新專案管理：執行階段.md：「在透過系統呈現的紀錄」→「再透過」
- redmine-mis/第23章 - [變革管理]MIS的新專案管理：監控階段.md：`memtion` / `Memtion` → `mention` / `Mention`（2 處）
- redmine-mis/第26章 - [ISMS管理]ISMS平台整體規劃.md：「資產盤風險評鑑」→「資產盤點、風險評鑑」；「此範為筆者」→「此範例為筆者」；「存屬巧合」→「純屬巧合」；「OPT VNP應用完成」→「OTP VPN應用完成」
- redmine-mis/第27章 - [ISMS管理]ISMS文管中心.md：`Redmin平台` → `Redmine平台`；「規定要的執行的管理表單」→「規定要執行的管理表單」
- redmine-mis/第28章 - [ISMS管理]ISMS表單紀錄的管理.md：「紙本的的PDF檔」去重字；「年度最重要活的活動」→「最重要的活動」；「拓樸圖示稽核必要的檢查項目」→「拓樸圖是稽核…」；「對應的條紋或附錄」→「條文」
- redmine-mis/第29章 - [ISMS管理]整理及管理分享.md：「重複謝的工作」→「重複性的工作」；「從作業效率來辦」→「來看」；「你有了(1)也)建構了(2)」移除多餘括號；`Even logs` → `Event logs`
- redmine-mis/第30章 - 初心、來時路及感謝：系列文章總結回顧.md：「擴展成成中小企業」去重字；「隱約約覺得」去重字

### SSDLC / 軟體外包規範 / MES（original）
- SSDLC.md：簡體字「盗用」→「盜用」；第 4 章兩個 `4.3` 重號，改為 `4.4`（加密模組鑑別）、`4.5`（非內部使用者之識別與鑑別）；第 7 章兩個 `7.2` 重號，「軟體及資訊完整性」改為 `7.3`；7.3 內文整段複製貼上重複，刪除重複片段
- 軟體外包規範.md：「資料異地備原」→「資料異地備援」；「緩衝區溢出」→「緩衝區溢位」
- mes-導入/MES 系統導入階段（Implementation Phase）.md：開頭寫「9 大階段」且列項與內文標題錯位，實際內文為 Phase 1–12。改為「12 大階段」，並依內文標題補齊 UAT、系統上線、上線後支援

### sop-bpmn（reprint，僅修明顯錯字）
- sop-bpmn/【SOP製作教學】新手適用，SOP範例、流程圖、製作流程全公開！.md：「IOS品質管理」→「ISO品質管理」；「這些都SOP的介紹都太複雜了」去贅字「都」；「標題面相」→「標題面向」
- sop-bpmn/【SOP製作教學】流程圖教學、重點範例、BPMN符號介紹！.md：「但其實還些事無法用」→「但其實還有些事無法用」
- sop-bpmn/【SOP製作教學】流程圖符號整理、BPMN2.0進階符號教學！.md：「電戶、帳戶名稱、收件地址」→「電話、…」；「隨著內建福號的不同」→「內建符號」

### 軟體專案任務挑戰賽（original）
- 軟體專案任務挑戰賽/關卡任務說明/角色職責說明.md：Tech Lead email `tech.lead@timetech.THROUGH.com` → `tech.lead@timetech.com`（誤代換殘留）
- 軟體專案任務挑戰賽/關卡任務說明/關卡一：團隊共識協定.md：移除標題下方孤立的「49」（Confluence 匯出殘留）
- 軟體專案任務挑戰賽/工具使用指南/什麼是 Jira ？如何開啟 Jira？.md：移除標題下方孤立的「35」；「追踪工作進度」→「追蹤工作進度」

---

## 疑似問題但未改

### 用語／風格（需使用者定奪）
- 系統評估/數位轉型從微服務開始.md、系統評估/數位轉型PPT.md、系統評估/RPA與WebAPI評估.md：三篇皆 `kind: original`，但通篇一致使用中國大陸用語（數據、模塊、接口、集成、質量、信息孤島、界面、人工智能、水平、響應）。屬全篇行文風格而非零星殘留，全面替換等同重寫，未動。若要統一為台灣用語請另行指示。
- redmine-mis 全系列：多處以「那些」作疑問代名詞（應為「哪些」），屬作者一貫書寫習慣，未做全文替換。

### 檔名／標題
- redmine-mis/第5章 - [Redmine]Redime系統邏輯說明.md：H1 與檔名皆誤植為「Redime」（應為 Redmine）。因不得新增／刪除檔案、無法同步改檔名，改標題會造成標題與檔名不一致，故保留。第8–14 章回顧連結沿用此原站標題，亦未動。

### 內容疑義
- redmine-mis/第29章：整理表 DAY 27 標為「ISMS文件管理」，第27章實際章名為「ISMS文管中心」。屬作者自撰回顧表述，未擅改。
- redmine-mis/第26章：「[被分配者]和[監派員]會收到mail通知」——Redmine 無「監派員」此功能名稱，疑為「監看者(watcher)」，但也可能指公司內指派主管，未改。
- redmine-mis/第22章：「已在規劃階段先接里程碑的工作項目先建立」——「先接」疑為「先按」或「先將」，兩種還原都說得通，未改。
- redmine-mis/第16章：「若成功發出現郵件測試成功的訊息」疑為「會出現」，未改。
- redmine-mis/第28章：「矯正處理單一定適合用線上紀錄的方式建立」依後文語氣疑漏「不」字，未改；「關注者議題的蒐集可以建立用小[專案]/[議題]的方案日常建立資料」語序錯亂但無法還原原意。
- redmine-mis/第27章：文管中心模組需求列「(X)議題追蹤」卻仍列「(O)甘特圖」，Redmine 甘特圖依賴議題資料，設定上略矛盾，屬作者設計選擇。
- redmine-mis/第26、20、30章：「資安改善：列的年度資安改善計畫」「顧問公司都有沒有想過」「在確保專案計畫及執行的前」「這件事就有不了了之」「我會帶這愧疚好好補償」——語句瑕疵但非明確錯字。
- redmine-mis/第2章 L84：稱「金管會在2022年11月已要求上市櫃公司要有專責的資安人員」，實際公告時間與分階段適用期程與此不完全吻合，但無法確定所指函令，未改。
- redmine-mis/第3章 L138：系統管理項目寫「流程」，Redmine zh-TW 介面多為「工作流程」。全篇用語一致，可能是作者版本譯名。
- redmine-mis/第4章 L83-87：圖號重複（先寫「圖19、20、21、22」，後又出現「圖20、21」「圖22」），該段只有 3 張圖，無法判定正確編號。
- redmine-mis/第5章 L54：`[http://網站IP/redmine/projects](http://xn--ip-xy0fp4h/redmine/projects)` 目標為中文被 punycode 化的假網址，連結實質無效；修法需改寫成純文字，屬結構調整。
- redmine-mis/第9章 L101：「請指定成員 (有設定成員才能被指派AR)」——本章主題為資訊服務申請，「AR」疑為第8章複製殘留。
- redmine-mis/第9章 L24「公司的八卦審」、L152「要不人就補MIS人力」；第14章 L31「但如何公司分開?」、L125「(部門公告會重要是發佈)」；第13章 L20「投入天為何?」；第12章 L115「非常很好」「欄位數計越多」——皆疑漏字／錯字，但還原方向不唯一。
- redmine-mis/第10章 L212-215：同段混用「[記錄時間]」與「[紀錄工時]」，實際 Redmine 只有一個按鈕，不確定作者版本譯名。
- redmine-mis/第12章 L103/105：連續兩行都寫「完成設定後[建立新議題]資料建立畫面如下」，第二行應為別的說明，無從還原。
- redmine-mis/第13章 L29/54：稱[活動]可查「10天內」紀錄，Redmine 預設為 30 天；可能是作者環境已調整設定。
- redmine-mis/第13章：本章功能重點列「日曆」，小節與路徑寫「行事曆」，同功能不同譯名，未強行統一。
- redmine-mis/第15章 KPI 表格：前三列公式寫「(需求／希望完成日 <= 實際完成日) 之件數 / 總申請件數」，依效率定義方向似乎相反；但作者已自陳「重點是精神和邏輯，並不是正不正確」。
- redmine-mis/第7章 L56：小標句尾多出「ISMS年度工作計畫」，與下方條列重複，疑為排版殘留。
- 軟體外包規範.md：小節標題「需求收集及疏離」疑為「梳理」或「釐清」；「資料保護機制」下裸列 `WCF / IPS / DMR / 防火牆 / HA`，`DMR` 疑為 `DMZ`、`WCF`（.NET 通訊框架）與其他網路名詞並列不搭。該節本身是未完成的關鍵字草稿，未動。
- SSDLC.md 6.1：引文寫「應採取較安全的傳輸協定(如 TLS1.1 以上)」。TLS 1.0/1.1 已於 RFC 8996 廢止，現行 NICS 防護基準為 TLS 1.2 以上。該段是引用官方查檢說明原文（可能引自舊版），建議自行核對來源版本。
- SSDLC.md：多處 PDF 複製殘留的半形空白斷字（「未經過適當的驗 證程序」「避免將詳細或除錯用訊息直接顯 示於使用者頁面」等）。屬格式雜訊、數量多，依「不重寫」原則未動。
- mes-導入/MES 顧問導入工具包（Consulting Toolkit）.md：Readiness Assessment 有 8 個評估項、每項 1–5 分，理論最低 8 分，但評分等級表寫「0~15 初級」，下界 0 不可能出現；「導入時間（典型）」表列 1+1~2+2+1+1 = 6~7 週，總計卻寫「6~8 週」。皆為輕微不一致，可能刻意留裕度。
- mes-導入/MES 系統導入顧問手冊.md：目錄章名與內文標題有出入（目錄「系統整合設計／MES 系統設定／客製需求與開發／使用者驗收測試／Go-Live 策略／KPI 管理」，內文為「系統整合／系統設定／客製開發／UAT／Go-Live／KPI」）。屬簡稱差異。
- mes-導入/MES 系統導入階段（Implementation Phase）.md：文末「導入 WBS 總覽」流程圖止於 Go-Live，未含 Phase 12 上線後支援。屬摘要圖取捨。
- 軟體專案任務挑戰賽/關卡任務說明/關卡一：團隊共識協定.md：檔名／標題為「團隊共識協定」，但內文三處指向 Jira 單「關卡一：團隊共識建立」。可能 Jira ticket 名稱本就不同，未統一。
- 軟體專案任務挑戰賽/關卡任務說明/關卡三：關鍵人物檔案.md：RACI 範例「需求釐清」列只有 A、C、C、I、I，無任何 R。文中原則僅要求「每個任務必須有一個 A」，且 A 可兼 R，未改。
- 軟體專案任務挑戰賽/關卡任務說明/關卡五：優先順序矩陣.md：「現有任務清單」有 8 項（含團隊培訓），但下方 Rovo AI 提示詞只列 7 項（缺團隊培訓）。屬提示詞範例取捨，未改。
- 軟體專案任務挑戰賽/時光膠囊計畫 - 解密中心.md：「六大關卡架構」下有一個只有表頭、內容全空的多餘表格（Confluence 匯出殘留）。屬格式問題，未動。

### 已確認無誤（不需修改）
- 全 `management/` 目錄外部網址格式皆正確，無 `https:/` 缺斜線問題。
- 全目錄相對路徑連結（僅 8 條，皆在 軟體專案任務挑戰賽/ 與 README.md）指向的檔案全部存在；`../images/` 下的圖片與 mp4 均存在。
- PMBOK 五大階段名稱／定義與第25章彙整表對第20–24章逐欄比對相符。
- ISO 27001 條文引用（本文 4–10 章、附錄 A.5–A.18、A.16 七個控制項等）皆正確；資通安全法 1–4 級事件定義正確。
- BPMN 2.0 事實查核：OMG 制定、2011 年發布 2.0、Event 空心圈、Exclusive「×」／Inclusive「○」／Parallel「+」／Complex「＊」、中間事件雙細線圈、Start 必為 Catching／End 必為 Throwing，皆正確。
- 艾森豪矩陣四象限與 Do First／Schedule／Delegate／Eliminate 對應正確。
- 時光膠囊「5 組密碼碎片／五個關卡」與表格「六大關卡」不衝突（關卡六為成果交付，非碎片關卡），60 分鐘與 6×10 分鐘一致。

---

## 統計

審核 54 篇，修改 32 篇，共 89 處
